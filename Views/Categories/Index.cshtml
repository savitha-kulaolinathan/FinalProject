@model IEnumerable<FinalProject.Models.Category>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<body class="backgroundImage">     
          <div class="container w-75 rounded p-1">

              @if (User.IsInRole("Admin"))
              {
              <div class="row pt-5 mb-4">
                  <div class="col-md-6">
                      <h2 class="font-weight-bold" style="color: #2E4756E6">List of Categories</h2>
                  </div>
                  <div class="col-md-6 text-right">
                      <a asp-area="" asp-controller="Categories" asp-action="Create" class="btn text-white" style="background-color: #2E4756E6; font-size: 20px"><i class="fas fa-plus"></i> &nbsp;Add new Category</a>

                  </div>                   

              </div>


                      @if (Model.Count() > 0)
                      {
                          <table class="table table-bordered table-hover text-white table-striped shadow-lg p-3 mb-5 rounded font-weight-bold" style="background-color:#33658A; font-size:20px;">

                              <thead>
                                  <tr class="text-center">
                                      <th>
                                          @Html.DisplayNameFor(model => model.Name)
                                      </th>
                                      <th>Action</th>
                                  </tr>
                              </thead>
                              <tbody>
                                  @foreach (var item in Model)
                                  {
                                      <tr>
                                          <td>
                                              @Html.DisplayFor(modelItem => item.Name)
                                          </td>
                                          <partial name="_TableButtonPartialView" model="item.Id" />
                                      </tr>
                                  }
                              </tbody>
                          </table>
                      }
                      else
                      {
                          <p> No category created yet</p>
                      }
                      }
                  </div>
    </body>
